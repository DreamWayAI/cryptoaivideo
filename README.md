# FastAPI File Upload to S3/R2

FastAPI бэкенд для загрузки больших файлов из Telegram в AWS S3 / Cloudflare R2.

## 🚀 Возможности

- ✅ Загрузка файлов из Telegram в S3/R2
- ✅ Генерация presigned URLs для доступа к файлам
- ✅ Health check endpoints
- ✅ Логирование всех операций
- ✅ Обработка ошибок
- ✅ Docker поддержка
- ✅ Готов к деплою на Railway

## 📋 Требования

- Python 3.10+
- AWS S3 или Cloudflare R2 bucket
- Railway аккаунт (для деплоя)

## 🔧 Установка локально

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Создайте `.env` файл:
   ```bash
   cp .env.example .env
   ```

4. Заполните переменные окружения в `.env`

5. Запустите сервер:
   ```bash
   uvicorn main:app --reload
   ```

Сервер будет доступен на http://localhost:8000

## 🌐 Деплой на Railway

### Вариант 1: Через GitHub

1. Загрузите код в GitHub репозиторий
2. Подключите репозиторий к Railway
3. Добавьте переменные окружения в Railway:
   - `S3_ENDPOINT`
   - `S3_BUCKET`
   - `S3_ACCESS_KEY`
   - `S3_SECRET_KEY`

4. Railway автоматически задеплоит приложение

### Вариант 2: Через Railway CLI

```bash
railway login
railway init
railway up
```

### Важно для Railway:

Railway автоматически использует **Procfile** для запуска приложения.
Если Procfile не работает, Railway использует Dockerfile.

## 📡 API Endpoints

### GET /
Базовая проверка работы сервера
```json
{
  "status": "ok",
  "message": "FastAPI сервер работает",
  "s3_configured": true
}
```

### GET /health
Health check endpoint
```json
{
  "status": "healthy",
  "s3_status": "configured"
}
```

### GET /test-s3
Тестирование подключения к S3
```json
{
  "status": "ok",
  "message": "S3 подключение работает",
  "bucket": "your-bucket",
  "endpoint": "https://...",
  "objects_count": 0
}
```

### POST /upload
Загрузка файла в S3

**Request:**
```json
{
  "file_url": "https://api.telegram.org/file/bot<TOKEN>/<FILE_PATH>"
}
```

**Response:**
```json
{
  "status": "ok",
  "filename": "uuid.mp4",
  "file_url": "https://presigned-url...",
  "bucket": "your-bucket"
}
```

## 🔗 Интеграция с n8n

### Пример workflow:

1. **Telegram Trigger** - получает видео от пользователя
2. **Get File Info** - получает file_path
3. **HTTP Request** - отправляет на `/upload` endpoint
4. **Send Message** - отправляет ссылку пользователю

## 📝 Переменные окружения

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `S3_ENDPOINT` | S3 endpoint URL | `https://bucket.s3.region.amazonaws.com` |
| `S3_BUCKET` | Название bucket | `my-bucket` |
| `S3_ACCESS_KEY` | AWS Access Key ID | `AKIAIOSFODNN7EXAMPLE` |
| `S3_SECRET_KEY` | AWS Secret Access Key | `wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY` |
| `PORT` | Порт сервера (автоматически в Railway) | `8000` |

## 🐛 Отладка

### Проблема: Railway показывает 404

**Решение:**
1. Убедитесь что `Procfile` присутствует в корне проекта
2. Проверьте что переменные окружения настроены
3. В Railway Settings → Build → убедитесь что Builder = Nixpacks (или Dockerfile)

### Проблема: "Invalid value for '--port': '$PORT'"

**Решение:**
1. Используйте `Procfile` (рекомендуется)
2. Или в Railway Settings → Deploy → Custom Start Command:
   ```
   sh -c 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'
   ```

### Проблема: S3 не подключается

**Решение:**
1. Проверьте правильность всех S3 переменных
2. Убедитесь что `S3_BUCKET` без расширения `.mp4`
3. Проверьте права доступа в S3

## 📚 Документация

После запуска сервера, откройте:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте логи в Railway: Deployments → Deploy Logs
2. Проверьте переменные окружения
3. Попробуйте endpoint `/test-s3` для диагностики

## 📄 Лицензия

MIT
