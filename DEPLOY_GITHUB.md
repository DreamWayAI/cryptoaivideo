# 🚀 Деплой через GitHub на Railway - Пошаговая инструкция

## Почему GitHub лучше?

✅ **Автоматический деплой** - при каждом push  
✅ **Версионирование** - откат к предыдущим версиям  
✅ **Совместная работа** - легко работать в команде  
✅ **CI/CD** - автоматические тесты и деплой  
✅ **Бесплатно** - и GitHub, и Railway имеют бесплатные планы

## 📋 Шаг 1: Подготовка файлов

У вас уже есть все необходимые файлы:
- `main.py` - основной сервер
- `requirements.txt` - зависимости
- `Dockerfile` - для Railway
- `.gitignore` - игнорируемые файлы
- `.env.example` - пример переменных

## 🐙 Шаг 2: Создание GitHub репозитория

### Вариант А: Через веб-интерфейс GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите зеленую кнопку "New" или "+"
3. Название: `telegram-video-server`
4. Описание: `Server for processing large video files via Telegram`
5. Выберите "Private" (приватный) или "Public"
6. НЕ добавляйте README, .gitignore (у нас уже есть)
7. Нажмите "Create repository"

### Вариант Б: Через GitHub CLI

```bash
gh repo create telegram-video-server --private
```

## 📤 Шаг 3: Загрузка кода на GitHub

```bash
# В папке с вашими файлами:

# 1. Инициализация git (если еще не сделано)
git init

# 2. Добавление всех файлов
git add .

# 3. Первый коммит
git commit -m "Initial commit: Video upload server for Telegram"

# 4. Добавление GitHub как remote
git remote add origin https://github.com/ВАШ_USERNAME/telegram-video-server.git

# 5. Пуш в GitHub
git push -u origin main

# Если ошибка с веткой, попробуйте:
git branch -M main
git push -u origin main
```

## 🚂 Шаг 4: Подключение Railway к GitHub

### 4.1 Создайте аккаунт Railway (если нет)
1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "Login with GitHub"
3. Авторизуйте Railway

### 4.2 Создайте новый проект
1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Первый раз - нажмите "Configure GitHub App"
4. Выберите ваш репозиторий `telegram-video-server`
5. Дайте доступ Railway к репозиторию

### 4.3 Railway автоматически:
- ✅ Обнаружит Dockerfile
- ✅ Начнет билд
- ✅ Задеплоит приложение

## 🔧 Шаг 5: Настройка переменных окружения

В Railway dashboard:

1. Выберите ваш проект
2. Нажмите на сервис
3. Перейдите в "Variables"
4. Нажмите "Raw Editor"
5. Вставьте:

```env
TELEGRAM_TOKEN=ваш_токен_telegram_бота
S3_BUCKET=название_вашего_bucket
S3_ENDPOINT=https://ваш-аккаунт.r2.cloudflarestorage.com
S3_ACCESS_KEY=ваш_access_key
S3_SECRET_KEY=ваш_secret_key
MAX_FILE_SIZE=2147483648
CHUNK_SIZE=5242880
```

## 🔴 Шаг 6: Добавление Redis

```bash
# В Railway dashboard:
1. Нажмите "New" в вашем проекте
2. Выберите "Database"
3. Выберите "Redis"
4. Railway автоматически подключит Redis
```

## 🌐 Шаг 7: Получение URL приложения

1. В Railway dashboard нажмите на ваш сервис
2. Перейдите в "Settings"
3. В разделе "Domains" нажмите "Generate Domain"
4. Получите URL типа: `telegram-video-server.up.railway.app`

## ♻️ Шаг 8: Настройка автодеплоя

Railway автоматически деплоит при каждом push в main!

```bash
# Внесли изменения в код?
git add .
git commit -m "Описание изменений"
git push

# Railway автоматически:
# 1. Получит изменения
# 2. Пересоберет Docker образ
# 3. Задеплоит новую версию
# 4. Переключит трафик
```

## 🔄 Обновление кода

### Добавили новую функцию?
```bash
# Отредактируйте main.py
git add main.py
git commit -m "feat: добавлена поддержка GIF"
git push
```

### Обновили зависимости?
```bash
# Отредактируйте requirements.txt
git add requirements.txt
git commit -m "chore: обновление зависимостей"
git push
```

### Изменили настройки Docker?
```bash
git add Dockerfile
git commit -m "fix: оптимизация Docker образа"
git push
```

## 📊 Мониторинг деплоев

В Railway dashboard вы увидите:
- ✅ Статус билда (Building/Deployed/Failed)
- 📝 Логи билда
- 📈 Метрики использования
- 🔄 История деплоев

## 🏷️ Использование веток для разработки

```bash
# Создание ветки для разработки
git checkout -b dev

# Работаете над новой функцией
git add .
git commit -m "wip: новая функция"
git push origin dev

# Готово к продакшену?
git checkout main
git merge dev
git push
```

## 💡 Советы

1. **Используйте .env локально, НЕ коммитьте его**
   ```bash
   cp .env.example .env
   # Редактируйте .env
   # .gitignore уже настроен игнорировать .env
   ```

2. **Semantic commits**
   - `feat:` - новая функция
   - `fix:` - исправление бага
   - `docs:` - документация
   - `chore:` - обновление зависимостей
   - `refactor:` - рефакторинг кода

3. **Проверяйте перед push**
   ```bash
   python main.py  # Локально работает?
   git status     # Что изменено?
   git diff       # Проверка изменений
   ```

4. **Откат при проблемах**
   ```bash
   # В Railway можно откатиться к предыдущему деплою
   # Settings → Deployments → Rollback
   ```

## 🎉 Готово!

Теперь у вас:
- ✅ Код на GitHub
- ✅ Автоматический деплой в Railway
- ✅ Redis для очередей
- ✅ Переменные окружения настроены
- ✅ URL для вашего сервера

## 🔗 Проверка работы

```bash
# Проверьте health endpoint
curl https://ваш-домен.up.railway.app/health

# Должен вернуть:
{
  "status": "healthy",
  "services": {
    "s3": "connected",
    "redis": "connected"
  }
}
```

## 🚨 Troubleshooting

### Build failed в Railway?
- Проверьте Dockerfile
- Проверьте requirements.txt
- Смотрите логи билда

### Redis не подключается?
- Убедитесь, что добавили Redis в проект
- Railway автоматически добавит REDIS_URL

### Переменные окружения не работают?
- Проверьте названия переменных
- Перезапустите сервис после изменения

---

**Готово!** Теперь каждый `git push` автоматически обновит ваш сервер! 🎉
